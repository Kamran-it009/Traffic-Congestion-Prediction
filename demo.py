import streamlit as st
# st.set_page_config(page_title="Traffic JAM Prediction", page_icon=":guardsman:", layout="centered")st.columns(st.columns(
col1, col2, col3 = st.columns(3)
with col1:
   st.button("A cat")

with col2:
   st.button("A dog")

with col3:
   st.button("A bear")